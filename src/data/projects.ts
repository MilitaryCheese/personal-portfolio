import type { Project } from '../types'

export const projects: Project[] = [
  {
    id: 'weathered',
    codename: 'PROJECT_01',
    name: 'Weathered',
    displayName: 'WEATHERED.APP',
    slug: 'weathered',
    year: '2025',
    status: 'live',
    roleType: 'Full stack',
    stack: [
      'React',
      'TypeScript',
      'Vite',
      'TanStack Query',
      'GSAP',
      'Framer Motion',
      'Tailwind CSS v4',
      'Node.js',
      'Vercel Serverless',
      'OpenWeatherMap API',
      'Gemini API',
      'Spotify API',
    ],
    stackReasoning: {
      React: 'Component-driven architecture across weather state views and music panel — no framework overhead needed for a focused single-page experience',
      TypeScript: 'Strict mode plus noUnusedLocals, noUnusedParameters, and erasableSyntaxOnly — the VisualStateKey template literal type enforces all 12 day/night × condition combinations at compile time',
      Vite: 'Dev server proxies /api to the local Node server so the frontend never knows which backend it is talking to — no local/production drift',
      'TanStack Query': 'Music query derives its cache key from weather query fields and gates on enabled: Boolean(weather) — sequences two async calls without a useEffect or manual loading state',
      GSAP: 'Continuous ambient gradient animation running entirely through CSS custom properties — color tweening stays off the React render cycle at 60fps',
      'Framer Motion': 'One-shot discrete transitions — staggered music card entrance, search bar focus scale, loading shimmer — cleanly separated from GSAP\'s continuous motion layer',
      'Tailwind CSS v4': 'CSS-first config with a hand-written design token layer — glassmorphic surface system shared across all cards without repeating utility classes',
      'Node.js': 'Business logic lives once in recommendationService.mjs, adapted by two thin HTTP entry points — one for local dev, one for Vercel — so production behaviour matches local exactly',
      'Vercel Serverless': 'API credentials never leave the server — Gemini and Spotify keys are server-only while only the read-only OpenWeatherMap key is exposed client-side',
      'OpenWeatherMap API': 'Current conditions and city geocoding — WeatherError carries a typed WeatherErrorCode union so retry logic pattern-matches on error type, not string messages',
      'Gemini API': 'Structured JSON output via responseMimeType: application/json — five-song curation validated against a shape check before enrichment, falls back to a condition-keyed static playlist on any failure',
      'Spotify API': 'Client credentials token cached at module scope with a 60-second expiry buffer — five track searches run in parallel via Promise.all, each degrading independently',
    },
    keyPatterns: [
      'Dual-runtime server architecture',
      'Derived query key chaining (TanStack Query)',
      'Partial failure as a first-class response shape',
      'CSS custom property animation (GSAP)',
      'Template literal TypeScript union types',
      'Exhaustive Record mapping at compile time',
      'Fan-out/fan-in async orchestration',
      'Per-step graceful degradation',
      'Discriminated union error types',
      'Ambient motion + discrete transition separation',
    ],
    architectureNote:
      'One business-logic module (recommendationService.mjs) powers two runtimes — a zero-dependency node:http server for local dev and a Vercel serverless function — via two thin adapters. The frontend proxies /api through Vite in dev and hits Vercel in production, with no code change at the call site.',
    demoUrl: 'https://weathered-app.vercel.app/',
    githubUrl: 'https://github.com/MilitaryCheese/weathered-app',
    thumbnail: 'https://lh3.googleusercontent.com/d/14IPMFphNQh8G2k1KFx5Qbdt7ZH7j70MJ',
    logline: 'A full-screen weather experience that sets the mood — then picks the soundtrack.',
    origin:
      'A deliberate subversion of the portfolio weather app cliché. The goal was to take the most overdone project idea and make it genuinely interesting — through visual craft, a structured AI music layer, and enough backend engineering to be worth reading the code.',
    approach:
      'Locked the visual system first — a 12-state gradient mapped to weather condition and time of day, driven by CSS custom properties so GSAP could animate color without touching React state. Wired the APIs once the design language was established. The server architecture came from a real constraint: Gemini and Spotify credentials needed to stay off the client, which forced the dual-runtime pattern.',
    proudOf: [
      'The sky\'s colors are never in React state — they\'re GSAP-owned CSS variables that React only hands a target palette to. 60fps color transitions with zero render cycles.',
      'RecommendationResponse always returns tracks — real or fallback — plus a structured issues[] array explaining exactly which upstream service degraded and why. The UI renders a working result and an honest status banner in the same response.',
      'The music query\'s cache key is derived directly from weather query fields and gated with enabled — two sequential async calls with no useEffect, no loading ref, no manual chaining.',
      'VisualStateKey is a template literal type built by concatenating two unions: `${"day"|"night"}-${WeatherCondition}`. The exhaustive Record<VisualStateKey, VisualStatePalette> means omitting any of the 12 palettes is a compile error.',
      'GrainOverlay is a pure SVG feTurbulence + feColorMatrix filter at mix-blend-overlay — one declarative element that makes every gradient feel atmospheric regardless of the palette underneath.',
    ],
    ifIRebuiltThis: [
      'Add navigator.geolocation as a starting point — manual city entry is the right fallback, not the default',
      'The orphaned api/spotify-token.ts file (an early client-side token approach that was abandoned) never got deleted — a small but honest gap in cleanup discipline',
      'No test suite was ever scheduled into the build phases — the staged delivery approach didn\'t leave room for it',
      'Mobile layout is functional but the gradient and grain system was designed for a widescreen canvas',
    ],
    challenges: [
      'Routing all enrichment through the server meant one hung Gemini or Spotify request could stall the entire response — solved with a fetchWithTimeout() AbortController wrapper at 12 seconds, applied to every outbound provider call',
      'GSAP context scoping under React StrictMode\'s double-invoke semantics — gsap.context(fn, element) with ctx.revert() in cleanup prevents orphaned tweens surviving a state change, which StrictMode makes non-trivial to get right',
      'The dual-runtime backend pattern required that recommendationService.mjs work as plain Node with no framework assumptions — no express, no Next.js request shape, nothing that would break under either adapter',
    ],
    whatPeopleSaid: [
      '"Why does rainy London sound like lo-fi hip hop?" — working as intended',
      '"I searched my hometown and it recommended death metal. It was raining." — also working as intended',
    ],
  },
  {
    id: 'meridian',
    codename: 'PROJECT_03',
    name: 'Meridian',
    displayName: 'MERIDIAN.APP',
    slug: 'meridian',
    year: '2026',
    status: 'live',
    roleType: 'Front-end heavy',
    stack: [
      'Next.js 16',
      'TypeScript',
      'Zustand',
      'TanStack Virtual',
      'Turborepo',
      'Tailwind CSS v4',
      'Binance WebSocket API',
    ],
    stackReasoning: {
      'Next.js 16': 'App Router shell — server component for fonts and metadata, single client component page as the composition root. No SSR of live data; the point was demonstrating client-side render performance under real-time load',
      TypeScript: 'Strict mode caught a stale ref bug that would have silently dropped price updates under load. No any anywhere in application code — confirmed by grep',
      Zustand: 'Three separate stores with selector-based subscriptions — a BTC price update never triggers a re-render in the ETH row. useShallow from zustand/react/shallow guards the symbol list selector against new array references on every tick',
      'TanStack Virtual': 'Absolute-positioned rows via transform: translateY(), overscan: 6 — verified 23 of 50 possible rows mounted in the DOM at once during a live browser check',
      Turborepo: 'Two workspaces: apps/meridian and packages/ui (@meridian/ui), resolved via npm workspaces as a real symlink. Forces a genuine boundary between domain components and generic visual primitives',
      'Tailwind CSS v4': 'CSS-first config with hand-rolled design tokens bridged into Tailwind via @theme inline — bg-surface, text-up, text-down work as ordinary utility classes. Explicit @source directive required to cross workspace boundaries for class generation',
      'Binance WebSocket API': 'Combined stream endpoint for all 50 symbols in one socket — no authentication, no API key, fires real price data fast enough to stress-test any rendering architecture',
    },
    keyPatterns: [
      'Normalised Zustand state with selector subscriptions',
      'Ref-based WebSocket message batching',
      'React.memo with custom per-problem comparators',
      'TanStack Virtual list rendering',
      'Concurrent React (useTransition, useDeferredValue)',
      'Separated domain / metrics / history stores',
      'Render instrumentation via module-scope Map',
      'Synthetic stress test through production data path',
      'Turborepo monorepo with shared UI package',
      'CSS compositor-layer price flash animations',
    ],
    architectureNote:
      'Incoming WebSocket messages queue in a useRef array and flush to Zustand via a single updateTickers() call every 100ms — reducing 200 raw events per second to 10 controlled render cycles. Verified by test: three messages within one 100ms window produce exactly one store subscription firing.',
    demoUrl: 'https://meridian-finance-app-meridian.vercel.app/',
    githubUrl: 'https://github.com/MilitaryCheese/meridian-finance-app',
    thumbnail: 'https://lh3.googleusercontent.com/d/1iQwWE32OyJB9zEpe9nplTMW1BH4TtefW',
    logline: 'A real-time market data platform built to handle the kind of event volume that breaks most React apps.',
    origin:
      'A deliberate exercise in React performance engineering at high event frequency. Cryptocurrency price feeds are one of the noisiest real-time public data sources available — if the architecture holds at 500 events per second, it holds anywhere. Built specifically to demonstrate the category of problem that real-time product teams solve daily.',
    approach:
      'Built the WebSocket batching pipeline and normalised Zustand store first with zero UI — no component rendered until the data layer was verified working. Virtualisation, memoised comparators, and concurrent rendering were layered on top once the pipeline was solid. The performance overlay was instrumented alongside the features, not retrofitted after.',
    proudOf: [
      'Two React.memo comparators solving genuinely different problems, with comments in the code explaining why. TickerRow\'s memo guards against virtualiser repositioning re-renders but explicitly cannot gate price-driven ones — the component subscribes to the store internally, and a hook-driven re-render bypasses memo regardless of props. PriceCell\'s comparator gates on Math.abs(next.price - prev.price) / base < 0.0001, verified by test to compare against last received props, not the original render.',
      'Three separate Zustand stores instead of one larger store — domain state, metrics telemetry, and price history kept on separate render paths. Subscribing to PerformanceOverlay\'s telemetry cannot accidentally share a render path with ticker price data.',
      'The stress test pushes synthetic events through the exact production updateTickers() path, not a separate demo mode — it\'s testing the real pipeline under artificial load, not a look-alike.',
      'useRenderCount increments inside the render body on purpose, with a comment: StrictMode\'s dev double-render should count as real cost being measured, not filtered out as noise.',
      'useStressTest reads live state via useTickerStore.getState() inside setInterval — the imperative escape hatch — so the component hosting the stress-test effect doesn\'t itself re-render at the tick rate.',
    ],
    ifIRebuiltThis: [
      'Add connection status UI — statusRef is tracked and returned from useWebSocket but nothing in the UI reads it. Discovered by grep mid-build, left as an honest gap rather than a hidden one',
      'Add error boundaries — a render-time exception takes down the whole tree with no recovery path',
      'Expand test coverage to TickerGrid, DetailPanel, Sparkline, PerformanceOverlay, and useStressTest — current tests cover the store, WebSocket batching, PriceCell, and TickerRow only',
      'Add fuzzy search — filter is a plain .includes() substring match on the symbol string',
      'Add CI — no GitHub Actions workflow or pre-commit hook runs tests or lint automatically',
    ],
    challenges: [
      'Tailwind v4\'s automatic content detection does not cross workspace boundaries — globals.css requires an explicit @source "../../../packages/ui/src" directive or classes used in packages/ui silently never get generated. A stale Turbopack cache masked this as a "module not found" error during build, requiring a .next cache wipe to diagnose',
      'Tuning the PriceCell memo comparator to skip floating point noise below 0.01% without swallowing real price movements — the threshold was empirically determined, not guessed, and the test confirms it compares against last received props rather than the original render',
      'React StrictMode double-invokes effects in dev, which closed the first WebSocket connection immediately on mount — exponential backoff reconnection (1000ms * 2^attempt, capped at 30s, reset on successful open) was verified to recover automatically without intervention',
    ],
    whatPeopleSaid: [
      '"The numbers are moving fast enough that I feel stressed looking at it" — accurate, by design',
    ],
  },
{
    id: 'studio-planner',
    codename: 'PROJECT_02',
    name: 'Studio Planner',
    displayName: 'PLANNER_STUDIO.APP',
    slug: 'studio-planner',
    year: '2025',
    status: 'live',
    roleType: 'Full stack',
    stack: ['React', 'TypeScript', 'Supabase', 'Framer Motion', 'PostgreSQL'],
    stackReasoning: {
      React: 'Drag-and-drop board state requires fine-grained component control',
      TypeScript: 'Caught three separate off-by-one bugs in the card reorder logic before they shipped',
      Supabase: 'Real-time subscriptions and a PostgreSQL backend without the overhead of a custom server',
      'Framer Motion': 'Card reorder animation needed spring physics, not a CSS transition',
      PostgreSQL: 'Relational data model for projects, allocations, and capacity rules',
    },
    keyPatterns: [
      'Real-time data synchronisation',
      'Drag-and-drop interaction',
      'Optimistic UI updates',
      'Conflict resolution',
      'Full-stack delivery',
    ],
    architectureNote:
      'Real-time sync via Supabase subscriptions with optimistic updates on the client — the UI updates immediately on drag, then reconciles with server state on confirmation.',
    demoUrl: 'https://studio-planner-pro.vercel.app/',
    githubUrl: 'https://github.com/MilitaryCheese/studio-planner-pro',
    thumbnail: 'https://lh3.googleusercontent.com/d/1EaJBnJIW5MMfUgkevIBzMXD3i8NdBqvh',
    logline: 'A capacity planning tool built out of frustration with every other capacity planning tool.',
    origin:
      'Running a design studio meant constantly answering the question of when I could take on the next project. No lightweight tool existed that modelled capacity the way I actually worked — as percentages across concurrent projects, not hours or tasks.',
    approach:
      'Prototyped the drag interaction and capacity model in an afternoon to validate the core concept. Spent the remaining time making real-time sync reliable and the animation feel genuinely satisfying rather than merely functional.',
    proudOf: [
      'The drag reorder animation has the right amount of physical weight to it',
      'Real-time sync across tabs just works — no manual refresh, no stale state',
      'The capacity model accurately reflects how creative studio work is actually structured',
    ],
    ifIRebuiltThis: [
      'Keyboard navigation was an afterthought — it deserves proper focus management',
      'Mobile drag interaction is unreliable and needs a touch-specific implementation',
      'The AI next-availability feature was scoped out for time — it would be the most valuable addition',
    ],
    challenges: [
      'Real-time conflict resolution when two browser tabs edit the same card simultaneously — a sync loop duplicated a card 200 times before the tab crashed, which is how I learned to love idempotent updates',
    ],
    whatPeopleSaid: [
      '"This actually feels nice to use"',
      '"Why is the empty state so sad"',
    ],
  }

]
