import type { Project } from '../../types'

interface ProjectPanelProps {
  project: Project | null
}

const ProjectPanel = (_props: ProjectPanelProps) => {
  return null
}

export default ProjectPanel
